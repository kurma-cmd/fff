import { useState } from 'react';
const key = (date: Date) => `${date.getFullYear()}-${String(date.getMonth() + 1).padStart(2, '0')}-${String(date.getDate()).padStart(2, '0')}`;
export function QadamDatePicker({ value, onChange }: { value: string; onChange: (value: string | null) => void }) {
  const initial = value ? new Date(`${value}T00:00:00`) : new Date(); const [open, setOpen] = useState(false); const [month, setMonth] = useState(new Date(initial.getFullYear(), initial.getMonth(), 1));
  const offset = (month.getDay() + 6) % 7; const count = new Date(month.getFullYear(), month.getMonth() + 1, 0).getDate(); const cells = Array.from({ length: offset + count }, (_, index) => index < offset ? null : index - offset + 1);
  const label = value ? new Date(`${value}T00:00:00`).toLocaleDateString('ru-RU', { day: 'numeric', month: 'short', year: 'numeric' }) : 'Добавить дату';
  return <div className={open ? 'qadam-date open' : 'qadam-date'} onBlur={event => { if (!event.currentTarget.contains(event.relatedTarget)) setOpen(false); }}><button type="button" className="qadam-date-trigger" onClick={() => setOpen(current => !current)}><span>▦</span><b>{label}</b></button>{open && <div className="qadam-date-menu"><header><button type="button" onClick={() => setMonth(new Date(month.getFullYear(), month.getMonth() - 1, 1))}>←</button><strong>{month.toLocaleDateString('ru-RU', { month: 'long', year: 'numeric' })}</strong><button type="button" onClick={() => setMonth(new Date(month.getFullYear(), month.getMonth() + 1, 1))}>→</button></header><div className="qadam-date-week">{['Пн', 'Вт', 'Ср', 'Чт', 'Пт', 'Сб', 'Вс'].map(day => <span key={day}>{day}</span>)}</div><div className="qadam-date-grid">{cells.map((day, index) => day ? <button type="button" className={`${value === key(new Date(month.getFullYear(), month.getMonth(), day)) ? 'selected' : ''} ${key(new Date()) === key(new Date(month.getFullYear(), month.getMonth(), day)) ? 'today' : ''}`} onClick={() => { onChange(key(new Date(month.getFullYear(), month.getMonth(), day))); setOpen(false); }} key={day}>{day}</button> : <span key={`empty-${index}`} />)}</div><footer><button type="button" onClick={() => { onChange(null); setOpen(false); }}>Очистить</button><button type="button" onClick={() => { const today = new Date(); onChange(key(today)); setOpen(false); }}>Сегодня</button></footer></div>}</div>;
}
